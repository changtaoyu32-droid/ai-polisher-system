# 项目进展文档

## 当前阶段：项目启动与规划（已完成）

- [x] 了解项目需求：创建一个名为“智能降低ai系统”的Web应用，包含多种降AI和学术润色角色，支持输入原文并进行风格转换。
- [x] 规划系统架构和界面设计（已完成）
- [x] 创建并完善极其详尽的实现计划（已更新至 implementation_plan.md）
- [x] 初始化项目结构（基于 Vite + Vanilla JS）
- [x] 实现前端 UI 界面（极致美感，暗黑科技风，毛玻璃效果）
- [x] 实现多种降AI提示词角色的生成与大模型接口调用
- [x] 实现本地文本对比与AI特征分析面板
- [x] 验证与调试

## 详细进展

- **2026-06-03**：
  - 项目初始化，明确降AI系统的 5 种核心提示词策略，设计包含 API 密钥配置、流式输出、差异对比 (Diff View) 和降AI率预估的交互界面。
  - 完成并更新了极其详尽的实现计划，涵盖本地降重正则替换库、编辑距离相似度算法、最长公共子序列差异对比逻辑以及流式 API SSE 读取的具体 JavaScript 代码 and CSS 样式设计。
  - 初始化了 Vite + Vanilla JS 构建环境，并完成了所有页面交互组件、大模型 SSE 客户端、本地降AI引擎、LCS 文本 Diff 算法 and 去重率圆环 of 编码。
  - 在本地 `http://localhost:5173/` 启动 Vite 开发服务器，进行了模拟改写、AI 词替换率检测、历史记录回填及双主题外观调测，全线功能通过验证。
  - **二轮与三轮交互调整（苹果极简主义化）**：
    - 针对明亮模式下输入框获得焦点变黑的 Bug 进行了完美修复，重新梳理明暗两套主题下的 `:focus` 和聚焦环阴影（苹果风极淡投影），消除视觉黑块。
    - **极简高度压缩**：根据用户反馈对策略选择器进行了收展优化，彻底移成了原本占用屏幕 200px 高度的庞大 Role Grid 卡片网格。将其改写为嵌入在控制条中的、苹果风格的极雅致自定义 `<select>` 下拉选项框，直接腾出了黄金垂直高度，保证了核心输入框首屏无缝展示。
    - **标签动态单行集成**：在下拉框右侧，设计了单行动态渲染标签区，仅在用户选择某一策略时，实时呈现该策略所对应的建议查重系统提示（如 CNKI知网、Turnitin、CopyLeaks 等）。既保留了醒目指引，又完美节省了空间。
    - **核心按钮居高突出**：将“智能降重改写”按钮与下拉框以及单行标签融合在一条仅 50px 高的优雅控制带中，放置在最核心的学术文本框上方，操作流程直观紧凑。
    - **字数统计缺陷归档**：修复了 triggerLocalDemo 在初始渲染演示文案时右侧未正确显示改写后字数的 Bug。
  - **四轮离线数据库扩充与配置求支集成**：
    - **极大扩充本地离线降重数据库**：在 [src/config.js](file:///c:/Users/19091/OneDrive/Desktop/论文降低ai小助手/src/config.js) 中丰富了 `LOCAL_REPLACEMENTS` 正则替换规则库。针对学术过渡句（如“随着...发展”、“不仅如此”、“主要是因为”等）、理工科高频动词冗余扩展（优化、运行、调用、创建、初始化等）以及名词/形容词学术化词汇进行全方位覆盖。即便无 API 也能展现优异的结构微调和查重去重效果。
    - **集成联系我获取 API 协助提示**：在 API 参数配置侧滑面板中，插入了微信联系人提示。
  - **五轮与六轮兜底算法增强与微信更新**：
    - **微信联系人更新**：微信协助人号码精细更新为 <b>17777259536</b>。
    - **极效兜底算法升级**：在 [src/engine.js](file:///c:/Users/19091/OneDrive/Desktop/论文降低ai小助手/src/engine.js) 中，为离线引擎增加了相似度兜底校验。若输入文本在经过正则字典词汇替换后，发现其与原文完全相同或变化微乎其微（编辑距离 < 3），系统自动触发基础字词同义替换，配合句首高端过渡语随机插入和句尾万能学术功能行为扩展，确保改写文本必定会产生合理的去重修改。
  - **七轮动态随机裂变引擎升级**：
    - **实现同义词多候选池随机匹配**：重构去重核心替换机制，将 LOCAL_REPLACEMENTS 升级为多候选同义词数组形式，实现本地离线降AI引擎的动态随机裂变。
    - **捕获组动态解析机制开发**：重写了 replace 循环体，支持实时捕获并动态注入正则捕获组占位符（如 `$1`、`$2` 映射）。
  - **八轮开源说明文档创建与 Git 本地仓库初始化**：
    - **编写 README.md 引导说明书**：在项目根目录下编写了精美的说明书，归纳了学术去重四大核心痛点、特色功能，并附带了详尽的本地 Vite 启动与大模型 API 连接配置教程。
    - **编写 .gitignore**：配置了 Git 忽略规则，将本地开发冗余（如 `node_modules`、`dist` 构建产物等）彻底排除。
    - **初始化并完成本地首次 Commit**：在本地成功执行 `git init`，暂存所有文件，并完成首次安全提交。
